import React, { useState } from 'react';
import withWrap from '../hocs/withWrap';

const HookPage = () => {
  return (
    <div>
      This is home
    </div>
  );
};

export default withWrap(HookPage);
